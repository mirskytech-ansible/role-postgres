58784

vserver!30!error_writer!filename = /var/log/cherokee/cherokee.error
vserver!30!logger!access!filename = /var/log/cherokee/cherokee.access

vserver!30!rule!120!document_root = /srv/www/scriptopia/webapp/site_static
vserver!30!match!domain!1 = staging*
vserver!30!nick = staging.scriptopia.co

source!11!host = 127.0.0.1:58784
source!11!interpreter = /usr/local/bin/uwsgi -s 127.0.0.1:58784 --ini /srv/www/scriptopia/uwsgi.ini




# http://stackoverflow.com/questions/21172312/how-to-loop-over-array-containing-template-variables-with-ansible

- vserver_number
- src_number
- project
- wsgi_port


vserver!{{ vserver_number }}!rule!701!disabled = 0
vserver!{{ vserver_number }}!rule!701!handler = redir
vserver!{{ vserver_number }}!rule!701!handler!rewrite!10!regex = .*
vserver!{{ vserver_number }}!rule!701!handler!rewrite!10!show = 0
vserver!{{ vserver_number }}!rule!701!handler!rewrite!10!substring = /s/script/template/
vserver!{{ vserver_number }}!rule!701!match = directory
vserver!{{ vserver_number }}!rule!701!match!directory = /template